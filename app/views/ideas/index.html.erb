<p style="color: green"><%= notice %></p>
<div class="d-flex justify-content-around p-3 align-items-center">
<h1>😳 Cursed emojis😳</h1>
<%= link_to "New cursed emoji", new_idea_path, { class: "btn btn-dark" } %>
</div>

<div id="ideas" class="d-flex flex-wrap">
  <% @ideas.each do |idea| %>
    <%= render idea %>
  <% end %>
</div>
